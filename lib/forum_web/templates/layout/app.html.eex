<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Forum</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/main.css") %>"/>
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>

  </head>
  <body>
    <main role="main" class="px-96 mt-8 flex flex-col justify-center">
      <header class="mb-8 relative">
        <%= if get_flash(@conn, :info) do%>
          <p class="bg-blue-100 border-2 border-blue-300 text-blue-900 text-center w-1/3 rounded-md px-2 py-2 absolute top-2 right-0" role="alert"><%= get_flash(@conn, :info) %></p>
        <% end %>
        <%= if get_flash(@conn, :error) do%>
          <p class="bg-red-100 border-2 border-red-300 text-red-500 text-center w-1/3 rounded-md px-2 py-2 absolute top-2 right-0" role="alert"><%= get_flash(@conn, :error) %></p>
        <% end %>
        <%= if assigns[:show_thread_list_button] || false do%>
          <%= link "< Back to threads list", to: Routes.thread_path(@conn, :index),
          class: "bg-gray-300 hover:bg-gray-400 border-2 border-black rounded-md px-1
          h-8 text-center pt-0.5 absolute top-2 left-0", method: :get %>
        <% end %>
        <p><h1 class="text-6xl text-center">Forum</h1></p>
      </header>
      <%= @inner_content %>
    </main>
  </body>
</html>
